warnings.js
• Create warnings.js and a separate window that points out issues that the program notices. 
• If advancement for a particular competition/group has non-sequential values (and is non-zero), throw a warning. 


• Tasks need to be reordered
• blank names in param 4 (won't allow a space).